export enum EnumDemo {
  DEMO_VALUE = "DEMO_VALUE",
}
export enum EnumFormatFile {
  VIDEO = "video",
  IMAGE = "image",
}

export enum EnumTypeButton {
  BRAND = "brand",
  DESTRUCTIVE = "destructive",
  GRAY = "gray",
}

export enum EnumColorButton {
  FILL = "fill",
  OUTLINE = "outline",
  GHOST = "ghost",
  LIGHT = "light",
  // DANGER = "danger",
}

export enum EnumSize {
  SMALL = "small",
  MEDIUM = "medium",
  BASE = "base",
  LARGE = "large",
}
export enum EnumSortValueTable {
  DESC = "DESC",
  ASC = "ASC",
}

export enum EnumFilterDateRangeType {
  ALL = "all",
  TODAY = "today",
  YESTERDAY = "yesterday",
  THIS_WEEK = "this_week",
  LAST_WEEK = "last_week",
  THIS_MONTH = "this_month",
  THIS_YEAR = "this_year",
  CUSTOM = "custom",
}

export enum EnumPermissionType {
  CREATE = "create",
  DELETE = "delete",
  READ = "read",
  UPDATE = "update",
}

export enum EnumPermissionFunction {
  PERMISSION = "api_permission",
  GIFT_CARD = "gift_card",
  PAYMENT_METHOD = "payment_method",
  PERMISSION_MANAGEMENT = "permission",
  QUOTATION = "quotation",
  RECONCILIATION = "reconciliation",
  ROLE = "role",
  SERVICE = "service",
  STATISTICS = "statistics",
  SUPPLEMENT = "supplement",
  USER = "user",
  VOUCHER = "voucher",
  VOUCHER_HISTORY = "voucher_history",
  WORK_SESSION = "work_session",
}
